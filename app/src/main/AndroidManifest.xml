<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.franc.mygest">

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.AllTransactionActivity"
            android:parentActivityName="com.example.franc.mygest.activities.MainActivity"
            android:label="@string/title_filter_activity"
            android:theme="@style/Theme.AppCompat.Light" />
        <activity
            android:name=".activities.AccountsManageActivity"
            android:label="@string/title_activity_crea_conto"
            android:theme="@style/Theme.AppCompat.Light"></activity>
    </application>

</manifest>